/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any specific configurations here if needed,
  // e.g., images: { domains: ['your-cdn-domain.com'] }

  // Next.js 14 App Router is configured by default.
  // Server Actions are enabled by default.
};

export default nextConfig;
